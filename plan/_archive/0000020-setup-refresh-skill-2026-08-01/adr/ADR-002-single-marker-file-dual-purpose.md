---
title: "ADR 002: Single marker file serves as both install-time record and refresh retry cache"
summary: "`.claude/.planifest-setup-flags` is the one file both setup.sh/setup.ps1 and the refresh skill read and write, rather than a separate install-time marker and a separate retry-cache file."
status: "accepted"
version: "0.1.0"
---
# ADR-002 - Single marker file serves as both install-time record and refresh retry cache

**Skill:** [adr-agent](../../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-sonnet-4-6
**Feature:** 0000020-setup-refresh-skill
**Component:** setup-hook-integration, planifest-refresh-setup
**Status:** accepted
**Date:** 2026-08-01

---

## Context

Two distinct needs emerged during P0: (1) `setup.sh`/`setup.ps1` should record the flags used at install time so future refreshes don't need to infer them from hook wiring (REQ-008), and (2) the refresh skill needs a way to recover if its own process is killed between confirming flags and completing setup, so an interrupted run doesn't strand the human on the loop with missing boot files and no cached state (REQ-009, REQ-010). This crosses two components (`setup-hook-integration` owns the install-time write; `planifest-refresh-setup` owns the refresh flow), so the data ownership and file-format decision needs to be recorded.

## Decision

Both needs are served by one file, `.claude/.planifest-setup-flags`, owned by `setup-hook-integration`. `setup.sh`/`setup.ps1` write it on successful install with `attemptStatus: completed`. `planifest-refresh-setup` writes to the same file (updating `attemptStatus: pending` plus the confirmed flags and attempted command) immediately before deleting `CLAUDE.md`/`AGENTS.md`, and a successful re-invocation flips it back to `completed`. No second cache file is created. The full schema is defined in `src/setup-hook-integration/docs/data-contract.md`.

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Two separate files (an install-time marker and a distinct refresh-retry cache) | Cleaner separation of concerns per producer | Two files that must stay in sync describing overlapping state (the same flag set); doubles the surface for staleness bugs between them | The state they'd hold is the same flag set for the same tool; splitting it invites drift for no benefit |
| Hold retry state only in the refresh skill's own session/conversation memory, no file at all | Simplest to implement | Does not survive a killed process, which is exactly the cross-session continuity gap this feature exists to close (REQ-009/REQ-010) | Fails the explicit Scope Lock Challenge requirement that retry state survive process death |
| A database or structured store instead of a flat file | More robust schema evolution | Massive overkill for a few hundred bytes of local, single-repo, uncommitted state; introduces a new dependency this feature's NFRs explicitly rule out | No new external dependencies is a hard constraint (execution-plan.md, confirmed design Constraints) |

## Affected Components

| Component | Impact |
|-----------|--------|
| `setup-hook-integration` | Gains write responsibility for `.claude/.planifest-setup-flags` on every successful install (REQ-008); becomes the data-owning component per the data contract |
| `planifest-refresh-setup` | Reads the file for reconstruction (REQ-002) and recovery (REQ-010); writes to it before deletion (REQ-009) |

## Consequences

**Positive:**
- One schema, one file, one source of truth, no synchronization problem between an install marker and a retry cache
- The recovery flow (REQ-010) is simple: read one file, check `attemptStatus`

**Negative:**
- `setup-hook-integration` and `planifest-refresh-setup` are now coupled through a shared file schema; a schema change to one requires updating both

**Risks:**
- If `setup.sh` and `setup.ps1` drift in how they write this file, reconstruction confidence degrades inconsistently between platforms; mitigated by R-003 in the risk register and REQ-008's parity acceptance criteria

---

## Related ADRs

- ADR-001 - related-to (this file is written before ADR-001's deletion step runs, enabling recovery from an interrupted deletion)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent. Path: `plan/current/adr/ADR-002-single-marker-file-dual-purpose.md`*

---
title: "ADR 004: Tool selection is explicit input, never silently auto-resolved"
summary: "The refresh skill requires the target tool to be named (by the human on the loop or asked for) rather than treating multiple detected tool installs as an error condition to guess through."
status: "accepted"
version: "0.1.0"
---
# ADR-004 - Tool selection is explicit input, never silently auto-resolved

**Skill:** [adr-agent](../../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-sonnet-4-6
**Feature:** 0000020-setup-refresh-skill
**Component:** planifest-refresh-setup
**Status:** accepted
**Date:** 2026-08-01

---

## Context

A repo can legitimately have more than one Planifest-managed tool installed at once (for example both `.claude/` and `.cursor/` present). An early draft of the error/sad-path Scope Lock answer treated this as a detection-time ambiguity that should halt the skill for clarification, the same halt-and-confirm pattern used for low-confidence flag reconstruction. During P0 review, the human on the loop corrected this framing: naming which tool to refresh is a normal input to the skill, the same way `setup.sh <tool>`/`setup.ps1 <tool>` already require a tool argument, not an error condition discovered partway through a run (see build-log.md, Scope Lock, Happy path amendment).

## Decision

The skill takes the target tool as an explicit input up front. If the human on the loop names a tool, the skill proceeds with that tool directly. If no tool is named and exactly one install is present, the skill proceeds with it without asking. If no tool is named and more than one install is present, the skill asks which tool before doing anything else, as the very first step, not as a failure branch reached after partial work. This is REQ-001, and it is deliberately positioned before REQ-002's detection logic even runs.

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Auto-detect and pick the "most likely" tool when multiple installs exist (e.g. most recently modified) | No question asked in the common single-tool case, transparent multi-tool case | Silently guessing which install to refresh when several are equally valid is exactly the kind of unconfirmed inference ADR-003 exists to prevent, applied one step earlier | Guessing which install to touch is worse than guessing a flag value, since it is wrong for an entire tool's worth of files, not one flag |
| Treat multiple installs as a hard error requiring the human to remove all but one before running the skill | Simple to implement | Punishes a legitimate, common repo layout (multiple tools intentionally installed side by side) | Multi-tool repos are valid and expected, not a misconfiguration to route around |
| Frame multi-install detection as part of the REQ-006 failure-handling path (as in the original draft) | Reuses the same failure-reporting machinery | Conflates "the human hasn't told me which tool yet" (a normal, expected input state) with "something went wrong" (an actual failure); makes the failure path do double duty for a non-failure case | The human on the loop explicitly flagged this conflation during Scope Lock review; corrected to treat tool selection as ordinary input (REQ-001), separate from REQ-006/REQ-007 |

## Affected Components

| Component | Impact |
|-----------|--------|
| `planifest-refresh-setup` | REQ-001 implements tool selection as the first step of the skill's flow, before any detection, confirmation, or deletion logic runs |

## Consequences

**Positive:**
- The skill's input contract matches `setup.sh`/`setup.ps1`'s existing convention (tool as an explicit argument), so the mental model carries over
- Multi-tool repos are supported cleanly, with one question asked only when genuinely needed

**Negative:**
- The skill's invocation surface has one more input to specify (or be asked) compared to a fully automatic "just figure it out" design

**Risks:**
- None beyond the general risk that a human on the loop names the wrong tool by mistake, which REQ-007 catches (no install found for that tool) rather than silently proceeding

---

## Related ADRs

- ADR-003 - related-to (both ADRs establish that ambiguity is resolved by asking, never by guessing)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent. Path: `plan/current/adr/ADR-004-explicit-tool-selection.md`*

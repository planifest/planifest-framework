# Recommendations - Setup Refresh Skill

**Skill:** [docs-agent](../../planifest-framework/skills/planifest-docs-agent/SKILL.md) (or any agent that identifies improvements)
**Tool:** claude-code
**Model:** claude-sonnet-4-6
**Feature:** 0000020-setup-refresh-skill
**Version:** 0.20.0

> Improvement suggestions discovered during the pipeline. These are not blockers, they are opportunities for future work. Each recommendation must be actionable and specific.

---

## Recommendations

| ID | Category | Priority | Component | Recommendation | Rationale | Effort |
|----|----------|----------|-----------|---------------|-----------|--------|
| REC-001 | testing | high | setup-hook-integration | Verify `Write-SetupFlagsMarker` (setup.ps1) and `refresh-delete-boot-files.ps1` with a live `pwsh` invocation on Windows or a pwsh-enabled CI runner before external release | Both were checked statically only in this development environment (no PowerShell runtime available); the bash side is live-tested and passing, but the PowerShell side carries residual risk (e.g. `ConvertTo-Json` depth/encoding quirks, per `src/setup-hook-integration/docs/quirks.md` Q-006) | small |
| REC-002 | correctness | high | setup-hook-integration | Fix `setup/copilot.sh`/`.ps1`'s `TOOL_HOOK_ADAPTER_DEST` self-copy bug (backlog `0000027`) | `setup.sh copilot` currently crashes on every invocation on any repo; discovered during this feature's P5 review, unrelated to this feature's own changes but a significant pre-existing functional defect | medium |
| REC-003 | testing | medium | setup-hook-integration | Add a live-invocation regression test that runs `setup.sh <tool>` end to end for every entry in `$VALID_TOOLS`, asserting exit 0 | The copilot crash (REC-002) went undetected because no existing test actually invokes `setup.sh copilot`; a full-matrix smoke test would catch this class of bug for any tool, not just copilot |
| REC-004 | maintainability | low | planifest-refresh-setup | Consider whether the tool-detection signal table in `SKILL.md` Step 1 should be extracted into a shared reference (it duplicates the tool-identification logic conceptually present in `setup.sh`'s `$VALID_TOOLS` handling), if a third consumer of "which tool is installed here" logic emerges | Currently only two places encode this (setup.sh's dispatch and the refresh skill's detection table); duplication is tolerable at two, but a third would be a signal to extract | small |

---

## Deferred Items

No items were deferred from this feature's scope (see `plan/current/scope.md`, both suggested actions from backlog `0000013` were pulled in and completed).

---

## Tech Debt

| ID | Component | Description | Impact if Ignored | Suggested Fix |
|----|-----------|-------------|-------------------|--------------|
| TD-007 | setup-hook-integration | `Write-SetupFlagsMarker` (setup.ps1) unverified by live execution in this codebase's development environment | A schema or encoding bug specific to PowerShell's `ConvertTo-Json` could ship undetected | Live `pwsh` test run per REC-001, before or shortly after this feature ships |

---

*Generated by docs-agent. Path: `plan/current/recommendations.md`*

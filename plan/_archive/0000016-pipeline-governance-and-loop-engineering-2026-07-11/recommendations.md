# Recommendations - Pipeline Governance and Loop Engineering

**Skill:** [docs-agent](../../planifest-framework/skills/planifest-docs-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-fable-5
**Feature:** 0000016-pipeline-governance-and-loop-engineering
**Version:** 0.16.0

> Improvement suggestions discovered during the pipeline. These are not blockers - they are opportunities for future work. Each recommendation must be actionable and specific.

---

## Recommendations

| ID | Category | Priority | Component | Recommendation | Rationale | Effort |
|----|----------|----------|-----------|---------------|-----------|--------|
| REC-001 | observability | high | structured-telemetry-mcp | Fix the `emit_event` envelope rejection (`(root): must be object`) observed live during this run (R-009) — likely the MCP tool schema declaring `event` without `type: object`, so string-serialized payloads reach validation | Loop/reversal telemetry (NFR-004) is inert until events actually land; Wave 1 promotion decisions depend on it | small |
| REC-002 | testing | medium | planifest-framework | Fix BSD/GNU portability in `tests/test-regression-pack.sh` and `tests/test-commit-msg-hook.sh` (`head -n -1` GNU-ism; pipefail SIGPIPE captures) — see `plan/backlog/0000001-flaky-test-suite-sigpipe/` | Full harness is red on macOS dev machines while green in CI, training humans to ignore failures | small |
| REC-003 | security | medium | planifest-framework | Implement same-changeset detection for `.ratchet-approve` in `ratchet-check.mjs` (ADR-004's "where detectable"), e.g. reject consumption when the marker is untracked/unstaged in git | Closes the marker-forgery gap from audit-detected to mostly-prevented (P5 medium finding) | medium |
| REC-004 | maintainability | medium | planifest-framework | Run the design-critic in report-only on the next 2 real features and record critique precision in P8, per the Wave 1 Ships When gate | The rollout-discipline constraint requires measured precision before any verifier gains blocking power | small (per feature) |
| REC-005 | maintainability | low | planifest-framework | Sweep remaining decomposition-sense "phase" wording in guide files (`feature-brief-guide.md`, docs prose) to "wave" | REQ-006 covered the template + orchestrator + spec-agent; guides may still carry the collision | small |
| REC-006 | observability | low | planifest-framework | Add a `ratchet_blocked` telemetry event distinct from run-log entries | P8 currently must parse run logs for attempted-weakening counts; a first-class event simplifies NFR-001 reporting | small |

---

## Deferred Items

Items from the scope document's "Deferred" list that should be revisited:

| Scope Item | Recommendation | When to Address |
|-----------|---------------|-----------------|
| `planifest-loop-designer` meta-skill | Build only after Wave 1 loop-precision evidence exists on ≥2 real features | After REC-004 data lands in two P8 reports |
| Cross-vendor critique automation for P1/P2 | Requires per-project model-access configuration; revisit alongside same-vendor evidence | After Wave 1 promotion decision |

---

*Generated by docs-agent.*

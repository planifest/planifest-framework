---
title: "ADR 002: product.yml with versionPolicy for Product-Level Versioning"
summary: "A root product.yml aggregates one release version across components via a three-value versionPolicy; single-component projects keep component.yml behaviour unchanged."
status: "accepted"
version: "0.1.0"
---
# ADR-002 - product.yml with versionPolicy for Product-Level Versioning

**Skill:** [adr-agent](../../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-fable-5
**Feature:** 0000016-pipeline-governance-and-loop-engineering
**Component:** planifest-framework
**Status:** accepted
**Date:** 2026-07-11

---

## Context

The framework versions single components (`component.yml`) but has no representation of one release version across multiple components, so multi-component consumer projects have no defined P9 tagging story. The concept originates from bug-bounty-hunter PR #4 and is adopted generically (concept only — no code copied).

---

## Decision

A `product.yml` at the project root: `id`, `name`, `version`, `versionPolicy`, `feature`, `components` (list of `{id, version}`). `versionPolicy` is a closed enum: `max-component-version` (derive = highest component version; the default), `explicit` (human-set), `external` (anchor dictates; integrates with External Anchor adoption mode). **Data ownership:** ship-agent writes/creates it at P9; orchestrator reads it at P0 for version suggestions. Projects with exactly one component need no `product.yml` — the existing `component.yml` path is the fallback, keeping this repo's own tagging unchanged.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Version list inside each component.yml | No new file | N components each claiming product version — no single owner, guaranteed drift | Violates single-ownership principle |
| Always derive max(component versions), no manifest | Zero config | No way to express explicit or externally-anchored versions | Real projects need the other two policies |
| Extend docs/about.md | File already read at P0 | about.md is living prose docs, not a machine contract; ship-agent would parse markdown | Wrong layer — manifests are YAML contracts |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| planifest-framework | New `product.template.yml`; ship-agent P9 read/create/validate + tagging derivation; orchestrator P0 step 3b reads it |

---

## Consequences

**Positive:**
- Multi-component projects get a deterministic, auditable tagging story with one owner
- Single-component projects (including this repo) are provably unaffected (NFR-003)

**Negative:**
- One more root manifest to keep consistent — component version bumps must be reflected in `components[]` at P9

**Risks:**
- Auto-creation guessing wrong on first multi-component use — mitigated by validation + human prompt on any invalid/lower version (REQ-004)

---

## Related ADRs

- None (foundational for REQ-003–005)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent.*

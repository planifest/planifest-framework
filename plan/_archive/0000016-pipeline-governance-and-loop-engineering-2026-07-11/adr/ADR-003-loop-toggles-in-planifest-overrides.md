---
title: "ADR 003: Per-Loop Toggles Live in planifest-overrides/loop-toggles.yml"
summary: "Loop enablement is user-owned, tool-agnostic project configuration — one key per loop, values off | report-only | on, absent file means all off."
status: "accepted"
version: "0.1.0"
---
# ADR-003 - Per-Loop Toggles Live in planifest-overrides/loop-toggles.yml

**Skill:** [adr-agent](../../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-fable-5
**Feature:** 0000016-pipeline-governance-and-loop-engineering
**Component:** planifest-framework
**Status:** accepted
**Date:** 2026-07-11

---

## Context

Resolves execution-plan Q-001. Each loop must be independently toggleable (`off | report-only | on`, default off — NFR-002 zero-regression). The toggle store needs to be: user-owned (agents must not flip their own loops on), tool-agnostic (Cursor/Windsurf/Cline have no .claude/settings.json), and feature-independent (toggles outlive any single pipeline run).

---

## Decision

`planifest-overrides/loop-toggles.yml` — one key per loop (`p0_completeness`, `design_critic`, `verify_by_execution`, `cross_model_review`, `reversal_protocol`), each `off | report-only | on`. Absent file or absent key = `off`. `planifest-overrides/` is already the user-owned directory the framework never writes to (component.yml exception: "Does not write to planifest-overrides/"), which makes agent self-enablement a hard-limit violation rather than a possibility. Skills read the file at loop entry; unreadable/invalid values are treated as `off` with a one-line warning.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| `.claude/settings.json` | Native hook config location | Claude-Code-specific; framework supports 6+ tools | Breaks tool-agnostic constraint |
| `plan/current/loop-toggles.yml` | Visible in the feature's plan | Archived/deleted at P7 — toggles would reset every feature | Wrong lifetime |
| `component.yml` field | Existing manifest | component.yml is framework-written; agents could self-enable | Violates user-ownership requirement |
| Environment variables | No file at all | Not git-visible, not auditable, per-shell drift | Fails auditability NFR-005 |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| planifest-framework | Loop-runner defines the read protocol; each loop skill checks its key at entry; setup.sh never creates the file (absent = off is the contract) |

---

## Consequences

**Positive:**
- Agents cannot enable their own loops; enablement is a deliberate, git-diffable human act
- Same file works across every supported tool

**Negative:**
- One more overrides file for humans to know about — documented in loop-runner and the template

**Risks:**
- Typo'd key silently means "off" — mitigated by the one-line warning on unknown keys

---

## Related ADRs

- ADR-007 - depends-on (deterministic enforcement reads the same file)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent.*

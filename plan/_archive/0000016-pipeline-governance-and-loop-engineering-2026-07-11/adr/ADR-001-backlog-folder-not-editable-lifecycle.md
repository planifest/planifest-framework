---
title: "ADR 001: Backlog Folder Instead of Editable Post-Archive Lifecycle"
summary: "Deferred work gets a durable plan/backlog/ home surfaced at the next P0; archived state stays locked — the editable-P7–P9 lifecycle from bug-bounty-hunter PR #4 is rejected."
status: "accepted"
version: "0.1.0"
---
# ADR-001 - Backlog Folder Instead of Editable Post-Archive Lifecycle

**Skill:** [adr-agent](../../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-fable-5
**Feature:** 0000016-pipeline-governance-and-loop-engineering
**Component:** planifest-framework
**Status:** accepted
**Date:** 2026-07-11

---

## Context

Phases regularly discover work that is real but out of scope for the active feature. Today it gets scope-crept in, silently dropped, or forces a same-run correction. The reviewed bug-bounty-hunter PR #4 solved this by keeping `plan/current/` editable through P7–P9 so late findings could be looped back into the shipping feature — which requires weakening the post-archive lock, the framework's strongest integrity guarantee.

---

## Decision

Adopt a `plan/backlog/{id}-{slug}/` folder: any phase agent may file a discovered-but-out-of-scope entry (problem, source feature/phase, date filed) without touching the active feature's scope. The orchestrator scans the backlog at every subsequent P0 and offers each entry to the human one at a time (pull-in / leave / discard). P7 remains the lock line — nothing archived is ever editable, and all loop/reversal machinery in this feature operates strictly within P0–P6 or as a gate immediately before P7.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Editable P7–P9 lifecycle (PR #4 design) | Late findings fixed in the same feature | Breaks the archived-state lock; resume detection ambiguity; ship-agent Hard Limit conflict | Highest-risk seam in the framework; explicitly rejected at P0 |
| Scope-creep into the active feature | No new mechanism | Uncapped features, design gate bypassed | Violates confirmed-design immutability |
| Informal notes (build-log only) | Zero machinery | Not surfaced anywhere; effectively dropped | The failure mode this feature exists to fix |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| planifest-framework | New template + orchestrator P0 pickup protocol; filing convention documented for all phase agents |

---

## Consequences

**Positive:**
- Non-blocking findings get a durable, human-reviewed home; archived state stays immutable
- Deferred work resurfaces automatically — forgetting requires an explicit human "leave/discard"

**Negative:**
- Fix latency: a backlog item waits at least until the next feature's P0

**Risks:**
- Backlog rot if the human habitually answers "leave" — mitigated by one-at-a-time presentation forcing a per-entry decision

---

## Related ADRs

- ADR-006 - related-to (maker-checker: backlog filing is agent-initiated but human-gated at pickup)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent.*

---
title: "ADR 005: Invalidation Cascades Larger Than 3 Artifacts Require Human Approval"
summary: "A granted reversal whose cascade invalidates more than 3 downstream artifacts always stops for the human, regardless of run mode."
status: "accepted"
version: "0.1.0"
---
# ADR-005 - Invalidation Cascades Larger Than 3 Artifacts Require Human Approval

**Skill:** [adr-agent](../../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-fable-5
**Feature:** 0000016-pipeline-governance-and-loop-engineering
**Component:** planifest-framework
**Status:** accepted
**Date:** 2026-07-11

---

## Context

Resolves execution-plan Q-003. REQ-019 mandates a human gate on "large invalidation cascades" but the threshold was undefined. The threshold must be deterministic (orchestrator control flow reads it, no judgement call) and small enough that a runaway correction cannot silently redo half the pipeline under continuous run.

---

## Decision

**Threshold: more than 3 invalidated artifacts** (requirement files, ADRs, or implementation units on the cascade list — the revised artifact itself does not count). Cascade size is computed from traceability and written into the verdict record *before* any re-work begins; if the list exceeds 3, the orchestrator stops for human approval regardless of run mode. The rationale for 3: it matches the framework's existing decomposition rule-of-thumb ("more than 3 user stories is too big") — a correction invalidating more than 3 artifacts is no longer a scoped delta, it is approaching a phase re-run, which is exactly what a human should sanction.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Percentage of total artifacts (e.g. >20%) | Scales with feature size | Small features gate at 1–2 artifacts (too tight), huge features at 10+ (too loose) | Absolute risk doesn't scale linearly with feature size |
| No threshold — assessor judges "large" | Flexible | Model judgement where determinism was required | Violates "skills advise, hooks/control-flow enforce" |
| Threshold 1 (every cascade gated) | Maximum safety | Makes continuous run meaningless for the reversal protocol | REQ-019 already gates every reversal in interactive mode; continuous needs headroom for genuinely small deltas |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| planifest-framework | Orchestrator reversal control flow compares cascade-list length to 3; verdict template records the count; REQ-019 gate list references this ADR |

---

## Consequences

**Positive:**
- "Large cascade" is now a number, not a vibe — enforceable by counting
- Worst-case silent rework under continuous run is bounded at 3 artifacts

**Negative:**
- A genuinely safe 4-artifact cascade still stops the run (accepted: erring toward the human)

**Risks:**
- Gaming by coarse cascade entries (bundling several revisions as one "artifact") — mitigated: cascade entries are file paths, and the ratchet hook still guards each individual write

---

## Related ADRs

- ADR-007 - depends-on (budget/caps enforcement family)
- ADR-006 - related-to (assessor computes the cascade the gate counts)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent.*

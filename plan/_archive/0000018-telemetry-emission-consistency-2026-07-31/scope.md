---
title: "Scope - 0000018-telemetry-emission-consistency"
summary: "Defines explicit boundaries of what is in scope and out of scope."
status: "active"
version: "0.1.0"
---
# Scope - 0000018-telemetry-emission-consistency

**Skill:** [spec-agent](../skills/spec-agent-SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000018-telemetry-emission-consistency
**Wave:** n/a (not waved — single pipeline run, 7 independent items)
**Version:** 0.18.0

---

## In Scope

- Removing the `--context-mode-mcp` coupling from telemetry hook installation in `setup.sh`/`setup.ps1` — `--structured-telemetry-mcp` alone becomes sufficient (req-001)
- Hook-driven emission (`emit-phase-start.mjs`, `emit-phase-end.mjs`, `context-pressure.mjs`) writing a durable failure marker on error, never blocking (ADR-005 unchanged) (req-002)
- Orchestrator checking the failure marker at phase-start checkpoints and surfacing an interactive block-or-proceed question once per distinct root cause per pipeline run; agent-driven emission failures stopping and asking immediately inline (req-003)
- Rewriting all 8 phase skills' Telemetry sections for immediate-interactive agent-driven failure, removing "skip silently if unavailable" language (req-004)
- A per-phase telemetry-activity record in `build-log.md` (req-005)
- Updating `telemetry-standards.md` as the single canonical reference for the unified signal and interactive-failure protocol (req-006)
- Elevating `discovery.md` (0000017 req-006) to Hard Limit status in `planifest-orchestrator/SKILL.md` — a self-audit finding from this feature's own P0, same failure class as the telemetry problem itself (req-007)

---

## Out of Scope

- `query_telemetry`/backend changes — confirmed fully functional this session via live write→read verification, not this repo's concern
- New telemetry event types beyond the existing 14
- Changes to `structured-telemetry-mcp` itself (the sibling MCP server repo)
- Unrelated loop toggles (cross-model review, reversal protocol, P0 completeness loop, etc.)
- A migration mechanism for legacy single-signal projects — no practical legacy install base exists (human-confirmed: effectively zero external users of this framework)

---

## Deferred

- None identified

---

*Generated by spec-agent. Path: `docs/scope.md`*

---
title: "Risk Register - 0000018-telemetry-emission-consistency"
summary: "Technical, operational, and security risks with their mitigations."
status: "active"
version: "0.1.0"
---
# Risk Register - 0000018-telemetry-emission-consistency

**Skill:** [spec-agent](../skills/spec-agent-SKILL.md) (updated by any agent that identifies a new risk)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000018-telemetry-emission-consistency
**Version:** 0.18.0
**Overall Risk Level:** medium

> Every entry must be specific to this feature. Do not produce generic risks.

---

## Risks

| ID | Category | Description | Likelihood | Impact | Mitigation | Status |
|----|----------|------------|------------|--------|-----------|--------|
| R-001 | technical | The failure-marker mechanism (req-002) could itself fail to write (disk full, permissions), and its *absence* after a known-attempted emission could be silently misread as success — reopening exactly the problem this feature exists to prevent | low | high — a broken marker mechanism defeats the entire point of req-003's detection logic | req-003's marker-check logic must distinguish "no marker because nothing failed" from "no marker because the marker write itself failed" wherever technically possible; document the residual gap explicitly if it cannot be fully closed | open |
| R-002 | operational | This feature touches 8 phase skills' Telemetry sections plus the orchestrator's own Resume Detection / phase-start logic — a moderate blast radius for one release, with duplicated logic across skills being a risk if not centralized | medium | low — framework tooling, fully test-covered per this repo's convention, but duplication increases future-drift risk | Keep the interactive-prompt logic defined once (orchestrator + telemetry-standards.md, req-003/req-006) and have the other 7 skills reference it rather than each re-implementing the check (req-004) | open |
| R-003 | technical | "Once per distinct root cause" (req-003) requires an identity/fingerprint check whose granularity could be wrong in either direction — too granular causes repeat prompts for what's really the same failure, too coarse collapses genuinely different failures into one prompt | medium | medium — either outcome undermines US-002's intent (either noisy, or masking a new distinct problem) | Define root-cause identity explicitly at P2 (ADR) using concrete fields (hook name + error type, or event type + error) rather than leaving it to implementer judgment at P3 | open |
| R-004 | process | `.claude/` (local generated skill/hook copy) has gone stale twice across two consecutive releases (0000017, 0000018) because `setup.sh` isn't re-run automatically after a skill ships — caught both times by chance, not enforcement | high (already recurred twice) | low — caught and fixed each time, but relies on someone noticing | Explicitly out of scope for this release (human-confirmed) — flagged here for a future backlog entry, not actioned now |
| R-005 | operational | req-007 (discovery.md Hard Limit) was picked up mid-P0 alongside the telemetry work — its own P2 ADR coverage needs to be produced alongside req-001-006's ADRs, not treated as an afterthought given it changes a Hard Limit (a consequential, "constrains future work" decision by the adr-agent's own criteria) | low | low — small, well-understood change, but skipping its ADR would be inconsistent with how this framework treats Hard Limit changes elsewhere | Ensure P2 produces a dedicated ADR for req-007, not just req-001-006 | open |

---

## Assumptions Logged as Risks

Documented assumptions from the specification are logged here with likelihood: medium.

| ID | Assumption | Impact if Wrong | Status |
|----|-----------|----------------|--------|
| A-001 | Removing the `--context-mode-mcp` coupling from telemetry hook installation has no other unstated technical dependency (shared directory, shared settings.json section, etc.) | Hook installation could fail in a new way for the `--structured-telemetry-mcp`-only path; needs verification at P3 by reading the actual installer code, not just the flag-gating condition | open |
| A-002 | "Once per distinct root cause" is implementable with a reasonably simple identity check without needing sophisticated fingerprinting | Either too many repeat prompts (over-granular) or too few (under-granular, collapsing distinct failures) | open |

---

*Generated by spec-agent. Updated by any agent that identifies a new risk. Path: `docs/risk-register.md`*

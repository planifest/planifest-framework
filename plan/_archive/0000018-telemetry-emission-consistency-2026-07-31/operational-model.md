# Operational Model - 0000018-telemetry-emission-consistency

**Skill:** [spec-agent](../skills/spec-agent-SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000018-telemetry-emission-consistency
**Version:** 0.18.0

> Framework tooling, not a runtime service: "operations" here means operating pipeline runs and enforcement hooks, not servers. No deployment topology, backups, or on-call rotation applies in the traditional sense; the human-on-the-loop is the escalation path — consistent with 0000016/0000017's precedent.

---

## Component Ownership

| Component | Owner (team/role) | On-call Rotation | Escalation Path |
|-----------|------------------|-----------------|-----------------|
| `planifest-framework/hooks/telemetry/*.mjs` | Framework maintainer (repo owner) | n/a — human-on-the-loop per session | Human operator in the active session |
| `planifest-framework/skills/*` (Telemetry sections, orchestrator Hard Limits) | Framework maintainer | n/a | Human operator in the active session |

---

## Runbook Triggers

| Trigger | Condition | Action | Automated? |
|---------|-----------|--------|-----------|
| Hook emission failure | A telemetry hook fails to POST or errors internally | Failure marker written (never blocks); surfaced at next phase-start checkpoint | yes |
| Agent-driven emission failure | An inline `emit_event` call fails during a phase skill's own work | Agent stops immediately, states the error, asks block-or-proceed | yes |
| Repeated identical failure | Same root cause recurs within one run, already acknowledged | Not re-asked — prior answer honored | yes |
| New distinct failure | A different root cause occurs in the same run | Asked fresh, independent of any prior acknowledged failure | yes |
| discovery.md missing/incomplete at P0 | Coaching about to begin without a populated discovery.md for the confirmed adoption mode | Pipeline error — orchestrator stops and writes it before proceeding (req-007) | yes |

---

## Alerting Thresholds

| Alert | Metric | Warning Threshold | Critical Threshold | Channel |
|-------|--------|-------------------|-------------------|---------|
| Telemetry failure markers | Failures per pipeline run | ≥ 1 (surfaced to human) | n/a | Build-log + inline prompt |
| Silent-gap phases | Phases missing a telemetry-activity record | ≥ 1 (should never occur post-fix) | n/a | Build-log audit (manual or P8) |

---

## Deployment Model

| Component | Strategy | Rollback Plan | Health Check |
|-----------|----------|--------------|-------------|
| Telemetry hooks + skill Telemetry sections | Copied into consumer projects via `setup.sh`/`setup.ps1` re-run | `git revert` on framework repo; consumers re-run setup | Framework test suite passes (req-001 through req-007) |

---

## Backup and Recovery

| Data Store | Backup Frequency | Retention | Recovery Time Objective | Recovery Point Objective |
|-----------|-----------------|-----------|------------------------|------------------------|
| `plan/` artifacts (design, requirements, discovery.md, build-log) | Every meaningful artifact write (git commit) | Full git history | Next session start (resume detection) | ≤ 1 artifact write |
| Failure marker file | Written on emission failure, cleared on acknowledgment | n/a — ephemeral by design | n/a | n/a |

---

*Generated by spec-agent.*

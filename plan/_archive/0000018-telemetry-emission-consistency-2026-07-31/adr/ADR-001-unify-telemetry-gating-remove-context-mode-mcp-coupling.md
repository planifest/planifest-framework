---
title: "ADR 001: Unify Telemetry Gating by Removing the --context-mode-mcp Coupling"
summary: "Removes the --context-mode-mcp AND-condition from telemetry hook installation — --structured-telemetry-mcp alone becomes sufficient, closing the exact gap that caused 0000017's telemetry loss. No migration mechanism built; no practical legacy install base exists."
status: "accepted"
version: "0.1.0"
---
# ADR-001 - Unify Telemetry Gating by Removing the --context-mode-mcp Coupling

**Skill:** [adr-agent](../../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-sonnet-5
**Feature:** 0000018-telemetry-emission-consistency
**Component:** planifest-framework
**Status:** accepted
**Date:** 2026-07-31

---

## Context

Two independent signals currently gate Planifest's telemetry emission: `.claude/telemetry-enabled` (a sentinel file written whenever `--structured-telemetry-mcp` is passed to `setup.sh`, gating agent-driven MCP `emit_event` calls) and a per-hook-embedded `PLANIFEST_TELEMETRY_URL` (written into each hook's command string in `.claude/settings.json`, but only when BOTH `--structured-telemetry-mcp` AND `--context-mode-mcp` are passed together — an existing gate). This feature's own P0 discovered directly that a project with only `--structured-telemetry-mcp` passed gets the sentinel but never gets the hooks wired at all — `phase_start`/`phase_end` telemetry silently never fires, indefinitely, with no way to notice short of reading `settings.json` by hand.

---

## Decision

Remove the `--context-mode-mcp` AND-condition from telemetry hook installation entirely. `--structured-telemetry-mcp` alone becomes sufficient to wire the hooks (with the backend URL embedded), matching the same single flag that already gates the sentinel. No migration mechanism is built for projects currently in the old coupled state, since no practical legacy install base exists for this framework (human-confirmed: effectively zero external users).

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Keep the coupling, add a migration/detection step warning a project in the sentinel-but-no-hooks state | Handles any hypothetical existing installs gracefully | Adds real complexity (detection logic, warning UX) to protect a population that doesn't practically exist | Rejected — no legacy base to protect, cost not justified |
| Remove the coupling, no migration step (this decision) | Simplest possible fix; directly closes the gap that caused 0000017's telemetry loss | Any hypothetical existing installation with the old partial config silently starts getting *more* telemetry than before (a strictly positive side effect, not a risk) | Chosen — smallest change that fully closes the gap |
| Merge the two signals into one entirely new config artifact (e.g. `.claude/telemetry-config.json`) read by both hooks and agent-driven checks | Cleaner unified abstraction going forward | Bigger surface change — touches the settings.json writer, hook readers, and the sentinel-check logic; not justified when a one-line coupling removal achieves the same practical outcome | Rejected — over-engineered relative to the actual gap found |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| `planifest-framework/setup.sh` | `install_telemetry_hooks` gating condition simplified to check `STRUCTURED_TELEMETRY_MCP` alone |
| `planifest-framework/setup.ps1` | Equivalent gating condition simplified |
| `.claude/settings.json` (writer behavior) | Now written whenever `--structured-telemetry-mcp` is passed, regardless of `--context-mode-mcp` |

---

## Consequences

**Positive:**
- Closes the exact gap that caused 0000017's telemetry loss, with the smallest possible change
- No new config format or detection logic to build or maintain

**Negative:**
- The two signals remain two technically different mechanisms (sentinel file vs. embedded URL) — now always set/unset together, but not literally merged into one artifact. A future reader could still be confused seeing two different-looking signals, even though they're now always consistent.

**Risks:**
- If a future feature adds a new telemetry-gated behavior, a developer could accidentally gate it on only one of the two (now-always-consistent) mechanisms and reintroduce a similarly-shaped gap. Mitigated by req-006 documenting both signals' relationship clearly in `telemetry-standards.md` as the canonical reference.

---

## Related ADRs

- None directly

---

## Supersedes

- None — this corrects an existing installer gating condition (0000009 vintage), not a formal decision reversal of a documented ADR.

## Superseded By

- None

---

*Generated by adr-agent.*

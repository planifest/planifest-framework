---
title: "ADR 003: discovery.md Elevated to Hard Limit Status"
summary: "discovery.md's existence-and-completeness requirement is elevated to Hard Limit status in planifest-orchestrator/SKILL.md, matching build-log.md's proven Hard Limit 8 pattern exactly — a self-audit finding from this feature's own P0, where the requirement was silently skipped for lack of enforcement teeth."
status: "accepted"
version: "0.1.0"
---
# ADR-003 - discovery.md Elevated to Hard Limit Status

**Skill:** [adr-agent](../../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-sonnet-5
**Feature:** 0000018-telemetry-emission-consistency
**Component:** planifest-framework
**Status:** accepted
**Date:** 2026-07-31

---

## Context

0000017 (req-006) introduced a structured P0 discovery pass, writing findings to `plan/current/discovery.md` before coaching begins, for every adoption mode. The instruction lived as step "3d" in `planifest-orchestrator/SKILL.md`'s Phase 0 Start Actions numbered list, with no enforcement mechanism. During this very feature's own P0 (0000018), the step was silently skipped — the orchestrator executed steps 0, 1, 2, 3c, then went straight into coaching, missing 3d entirely, and only caught the omission by chance while writing `design.md`. This is the identical failure class as 0000018's own core problem: a correctly-specified requirement with no enforcement, silently skipped, unnoticed.

---

## Decision

Elevate discovery.md's existence-and-completeness requirement to Hard Limit status in `planifest-orchestrator/SKILL.md`, matching the exact pattern already used for `build-log.md` (Hard Limit 8): stated prominently in the Hard Limits list at the top of the skill file, with explicit teeth ("a missing or incomplete discovery.md before coaching begins is a pipeline error — stop and write it before proceeding"), cross-referenced back at Phase 0 Start Actions step 3d (matching how step 2 already cross-references Hard Limit 8), and added as a redundant catch in the Phase 0 → Phase 1 Gate Checklist.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Leave discovery.md as a numbered sub-step, rely on agent diligence | No change needed | Already demonstrated to fail in this very session — the exact bug being fixed | Rejected — proven insufficient by direct, immediate counter-example |
| Add only a Gate Checklist item, without a Hard Limit | Catches the omission before design.md is presented, cheaper | Only catches it at the END of P0 coaching, not at the moment it should have happened; doesn't carry the same "stop immediately" weight build-log.md's Hard Limit has | Rejected as insufficient alone — kept as a supplementary belt-and-suspenders addition, not a replacement |
| Hard Limit + step cross-reference + Gate Checklist item, matching build-log.md's full pattern (this decision) | Matches an already-proven-effective enforcement pattern exactly (build-log.md entries are never missed); triple redundancy | Slightly more verbose skill file | Chosen — proven pattern, full redundancy for a requirement that already demonstrated it can silently fail |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| `planifest-framework/skills/planifest-orchestrator/SKILL.md` | New Hard Limit entry; Phase 0 Start Actions step 3d cross-references it; Phase 0 → Phase 1 Gate Checklist gets a new item |

---

## Consequences

**Positive:**
- discovery.md gets the same enforcement rigor as build-log.md, which has never been missed since becoming a Hard Limit
- Closes a self-identified real gap from this very session

**Negative:**
- Adds another entry to an already-long Hard Limits list — marginal increase in skill file length/complexity, though matching an established, working pattern

**Risks:**
- None significant — this is a low-risk, mechanical elevation of an existing (correct) requirement's enforcement level, not a new behavior

---

## Related ADRs

- ADR-004 (0000017) Structured P0 Discovery Pass and discovery.md Lifecycle - extends (adds enforcement to a mechanism that ADR already defined the content/lifecycle of)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent.*

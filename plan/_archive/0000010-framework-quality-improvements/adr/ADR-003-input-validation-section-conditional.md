---
title: "ADR 003: Input validation section in requirement template is conditional, not mandatory"
summary: "The ## Input Validation section added to requirement.template.md is explicitly conditional — required only for requirements that read untrusted external content into model-visible output. Making it mandatory for all requirements would produce noise in requirements where no external input exists."
status: "accepted"
version: "0.1.0"
---
# ADR-003 - Input validation section in requirement template is conditional, not mandatory

**Skill:** planifest-adr-agent
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000010-framework-quality-improvements
**Component:** planifest-framework/templates
**Status:** accepted
**Date:** 2026-05-12

---

## Context

Feature 0000009's S-001 security finding (prompt injection via `featureId` read from filesystem) had a root cause in the spec: `req-008-orchestrator-presence-check.md` did not include an input validation acceptance criterion for the `featureId` field. The P8 build report recommended adding an input validation AC template to `requirement.template.md`.

The design question: should the `## Input Validation` section be mandatory in every requirement file, or conditional (only included when the requirement involves untrusted external input)?

---

## Decision

The `## Input Validation` section is conditional. It is included in a requirement file only when the requirement involves reading content from an untrusted source (filesystem, hook stdin, environment variables, HTTP responses) and using that content in output visible to the model or end user (banners, logs, displayed text, model context injection).

The template marks the section with a conditional comment. The spec-agent instruction (also in the template) says: "Include this section whenever the requirement reads untrusted content and uses it in output visible to the model or user."

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Mandatory for all requirements | No judgement call required; uniform requirements | Most requirements have no external input; section would be empty or N/A for the majority; adds noise | Cargo-cult compliance — adds form without substance |
| Mandatory only for security-tagged requirements | Explicit tagging triggers the section | Requires a security tag field to be added to all requirements; adds complexity | Over-engineered for the problem |
| Keep as conditional (chosen) | Applied where relevant; no noise elsewhere | Relies on spec-agent to correctly identify requirements needing it | Appropriate: spec-agent instructions + template comment provide the trigger |
| Separate security requirement file type | Clean separation | Duplicates content already in the functional requirement; two files for one feature | Unnecessary duplication |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| planifest-framework/templates/requirement.template.md | Gains `## Input Validation` section with conditional comment |
| planifest-spec-agent/SKILL.md | Should reference this ADR when deciding whether to include the section (note: spec-agent SKILL.md update is out of scope for this feature — the template comment is the primary trigger) |

---

## Consequences

**Positive:**
- Future requirements that read filesystem content will have explicit input validation ACs, closing the spec gap that caused S-001
- Existing requirement files that don't involve external input are unaffected
- The pattern is documented with a concrete example in the template, so spec-agents have a reference

**Negative:**
- The spec-agent must exercise judgement to identify which requirements need the section — there is no automated enforcement at requirement-write time
- A spec-agent that misses an applicable requirement will produce the same gap as the one that caused S-001

**Risks:**
- The conditional instruction may be ignored by a spec-agent under context pressure; mitigated by placing the instruction prominently in the template and by the security review (P5) catching any missed validations at implementation time

---

## Related ADRs

- None (no prior ADRs in this feature at time of writing)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent. Path: `plan/current/adr/ADR-003-input-validation-section-conditional.md`*

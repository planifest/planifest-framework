# Cost Model - 0000017-ratchet-forgery-detection-and-telemetry-schema-spec

**Skill:** [spec-agent](../skills/spec-agent-SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000017-ratchet-forgery-detection-and-telemetry-schema-spec
**Version:** 0.17.0

> No cloud infrastructure — the cost drivers are model tokens consumed during the pipeline run itself, primarily the Scope Lock suggested-answers subagent dispatches (req-005) and codegen subagent dispatches at P3.

---

## Summary

| Category | Estimated Monthly Cost | Notes |
|----------|----------------------|-------|
| Compute | $0 | Local hooks/scripts (Node), no hosted runtime |
| Storage | $0 | Plain files in git |
| Network / Egress | $0 | No network calls introduced by this release |
| Third-party Services | Token spend, per run (below) | Model usage is the only real cost |
| **Total** | **≈ token spend only** | |

---

## Model-Token Cost Drivers (per pipeline run)

| Driver | Mechanism | Marginal cost profile | Bound |
|--------|-----------|----------------------|-------|
| Scope Lock suggested-answers agent (req-005) | One subagent dispatch per scenario-path question per item, only on explicit human request | Low-medium per dispatch; scales with item count × 4 questions | Only triggered on request, never pre-drafted automatically |
| Codegen subagents (P3, all 6 requirements) | One dispatch per requirement's implementation | Medium — proportional to file/skill-content size touched | Model Tier Decision Table routes mechanical work to cheaper tier where applicable |
| Docs-agent sweep (req-003) | Single dispatch reviewing all in-scope paths for Phase/Wave instances | Medium — proportional to corpus size (`docs/`, `planifest-framework/`, README) | One pass, not iterative |

**Zero-config cost:** this release adds no new recurring cost — all spend is one-time, incurred during this pipeline run only.

---

## Assumptions

1. `planifest-scope-lock-agent` is invoked on-demand only (per req-005's confirmed design), not pre-emptively for every question — bounds token spend to actual human requests
2. Regression suite execution (req-001) runs on existing CI infrastructure with no new compute provisioned

---

*Generated by spec-agent.*

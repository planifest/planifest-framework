# Operational Model - 0000017-ratchet-forgery-detection-and-telemetry-schema-spec

**Skill:** [spec-agent](../skills/spec-agent-SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000017-ratchet-forgery-detection-and-telemetry-schema-spec
**Version:** 0.17.0

> Framework tooling, not a runtime service: "operations" here means operating pipeline runs and enforcement hooks, not servers. No deployment topology, backups, or on-call rotation applies in the traditional sense; the human-on-the-loop is the escalation path — consistent with 0000016's precedent.

---

## Component Ownership

| Component | Owner (team/role) | On-call Rotation | Escalation Path |
|-----------|------------------|-----------------|-----------------|
| `src/context-mode-hooks` (block-bash/grep/webfetch) | Framework maintainer (repo owner) | n/a — human-on-the-loop per session | Human operator in the active session |
| `planifest-framework/skills` (orchestrator, scope-lock-agent) | Framework maintainer | n/a | Human operator in the active session |

---

## Runbook Triggers

| Trigger | Condition | Action | Automated? |
|---------|-----------|--------|-----------|
| Ratchet weakening detected | Weakening write during an active loop/reversal | Blocked (exit 2) unless a committed `.ratchet-approve` exists for that path | yes |
| Uncommitted approval found | Weakening edit attempted while `.ratchet-approve` exists but is uncommitted | Blocked with an explicit message naming the pending path; approver told to commit first | yes |
| Node runtime missing (setup) | `.mjs` hook install attempted without Node available | Clear fail message at setup time, before wiring | yes |
| Node runtime missing (runtime) | Hook invocation attempted without Node available | Clear fail message explaining enforcement did not fire; tool call still proceeds (fail-open) | yes |
| Partial discovery-pass failure | A discovery signal (e.g. malformed `package.json`, corrupted archive entry) can't be read | Affected `discovery.md` section states it couldn't be determined; coaching proceeds on the rest | yes |
| Discovery.md missing/incomplete on resume | Session resumes mid-pipeline-run and discovery.md is absent or partial | Regenerated fresh, not patched | yes |

---

## Alerting Thresholds

| Alert | Metric | Warning Threshold | Critical Threshold | Channel |
|-------|--------|-------------------|-------------------|---------|
| Ratchet weakening attempts | Blocks per feature | ≥ 1 (each surfaced) | ≥ 3 (pattern — review agent behavior) | Run log + P8 report |
| Uncommitted approval blocks | Backstop triggers per feature | ≥ 1 (surfaced to approver) | n/a | Audit log |
| Missing-runtime fail-open events | Occurrences per session | ≥ 1 (surfaced to human) | n/a | Setup/runtime message |

---

## Deployment Model

| Component | Strategy | Rollback Plan | Health Check |
|-----------|----------|--------------|-------------|
| `src/context-mode-hooks` (`.mjs` ports) | Copied into consumer projects via `setup.sh`/`setup.ps1` re-run | `git revert` on framework repo; consumers re-run setup | Cross-platform hook test suite passes (req-004) |
| `planifest-scope-lock-agent` skill | Copied into consumer projects via `setup.sh` re-run | `git revert` on framework repo | Scope Lock Challenge dry-run per rigor spec |

---

## Backup and Recovery

| Data Store | Backup Frequency | Retention | Recovery Time Objective | Recovery Point Objective |
|-----------|-----------------|-----------|------------------------|------------------------|
| `plan/` artifacts (design, requirements, discovery.md, build-log) | Every meaningful artifact write (git commit) | Full git history | Next session start (resume detection) | ≤ 1 artifact write |
| `.ratchet-approve` / permanent audit log | Every consumption event | Full git history | n/a | 0 — consumption is atomic with the audit-log write |

---

*Generated by spec-agent.*

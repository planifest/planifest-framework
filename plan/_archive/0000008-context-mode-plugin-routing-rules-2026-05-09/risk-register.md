---
title: "Risk Register - context-mode plugin routing rules"
summary: "Technical, operational, and security risks with their mitigations."
status: "active"
version: "0.1.0"
---
# Risk Register - context-mode plugin routing rules

**Skill:** planifest-spec-agent
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000008-context-mode-plugin-routing-rules
**Version:** 0.1.0
**Overall Risk Level:** low

---

## Risks

| ID | Category | Description | Likelihood | Impact | Mitigation | Status |
|----|----------|------------|------------|--------|-----------|--------|
| R-001 | operational | Non-Claude Code tools (Cursor, Windsurf, Copilot, Cline) relied on the `AGENTS.md` routing rules file produced by setup. If those tools cannot receive routing rules via the plugin's system prompt, agents on those tools will use native tools (Grep, Bash, WebFetch) and flood their context windows. | low | medium | Explicitly deferred in scope.md; deferred item blocks until plugin compatibility with each tool is confirmed. Document current state in `docs/context-mode.md` Supported Tools table. | open |
| R-002 | operational | Codex users who ran setup with `--context-mode-mcp` previously received routing rules in `AGENTS.md`. After this change, re-running setup will no longer write routing rules to `AGENTS.md`. Codex cannot receive plugin system prompt. | low | medium | Explicitly deferred in scope.md. Codex routing rules fallback is a separate future work item. | open |
| R-003 | technical | Residual references to `context-mode-agents.md` or routing rules copy step in files not covered by this change could cause confusion or re-introduction of the removed behaviour on a future setup run. | low | low | NFR-001 requires a grep audit confirming zero stale references in live files before ship. Plan archive docs are explicitly excluded — they cannot affect runtime behaviour. | open |

---

## Assumptions Logged as Risks

| ID | Assumption | Impact if Wrong | Status |
|----|-----------|----------------|--------|
| A-001 | The context-mode plugin injects routing rules for all tools that support the plugin system, not just Claude Code | Non-Claude Code tool users lose routing rules with no fallback; mitigated by R-001 and deferred scope item | open |
| A-002 | `AGENTS.md` in the project root is used exclusively as a Codex boot file, not as a context-mode routing rules file by any active user | Codex users relying on routing rules silently lose them; mitigated by R-002 and deferred scope item | open |

---

*Generated by spec-agent.*

---
title: "Scope - context-mode plugin routing rules"
summary: "Defines explicit boundaries of what is in scope and out of scope."
status: "active"
version: "0.1.0"
---
# Scope - context-mode plugin routing rules

**Skill:** planifest-spec-agent
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000008-context-mode-plugin-routing-rules
**Version:** 0.1.0

---

## In Scope

- Delete all 5 `context-mode-agents.md` files (framework template + 4 skill asset copies)
- Remove the routing rules file copy step from `setup.sh` and `setup.ps1`
- Update `--context-mode-mcp` help text in both setup scripts to describe enforcement hooks only
- Update `docs/context-mode.md` — remove Routing Rules install step, update Supported tools table
- Update `planifest-framework/tool-setup-reference.md` — flag description, Creates section, Context-Mode Integration section
- Update `planifest-framework/getting-started.md` — Option: Context-Mode description
- Update `planifest-framework/templates/standard-boot.md` — remove AGENTS.md reference from context-mode directive
- Update the 4 installed skill copies of `standard-boot.md` to match
- Update `CLAUDE.md` — same context-mode directive change

---

## Out of Scope

- Enforcement hooks (`block-grep.sh`, `block-bash.sh`, `block-webfetch.sh`) — not changing, not removing
- `AGENTS.md` root file — not a routing rules file; Codex uses it as a boot file
- `planifest-framework/templates/standard-boot.md` for non-Claude Code tools that do not support the plugin
- Plan archive documents — accurate at time of writing; retroactive edits have no value
- Any changes to the context-mode plugin itself

---

## Deferred

- Per-tool routing rules fallback for tools that do not support the plugin system (Cursor, Windsurf, Copilot, Cline) — if these tools cannot receive routing rules via plugin, a separate mechanism will be needed; blocked until confirmed whether those tools support the plugin's system-prompt injection

---

*Generated by spec-agent.*

---
title: "Scope - 0000019-self-description-and-session-hygiene-fixes"
summary: "Defines explicit boundaries of what is in scope and out of scope."
status: "active"
version: "0.1.0"
---
# Scope - 0000019-self-description-and-session-hygiene-fixes

**Skill:** [spec-agent](../skills/spec-agent-SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000019-self-description-and-session-hygiene-fixes
**Wave:** 1 (single wave — human directive)
**Version:** 0.19.0

---

## In Scope

- req-001 (0000014): remove README Count column, correct structure-diagram paths, align README:42 and the setup/ row.
- req-002 (0000015): `component.json` → `component.yml` in CI workflow and both shipped hooks, plus two new pass/fail tests run against the hooks directly.
- req-003 (0000016): reword the CI parity guarantee (presence heuristic, not correspondence) in hook strings and README.
- req-004 (0000017): reword README Hard Limit 1 to enforceable, falsifiable behaviour; align orchestrator wording.
- req-005 (0000018): new repository-scoped CI script verifying structure-diagram paths and folder-table coverage, run on PR — depends on req-001.
- req-006 (0000011): local-timestamp, `//`-delimited design-confirmation format in the design template and orchestrator skill.
- req-007 (0000012): orchestrator `/clear` at Phase 0 start and P9 completion, plus advisory dynamic compaction monitoring.
- req-008 (0000026): document the backlog ID sequence convention in the template and orchestrator P0 step.
- **0000027 (context-pressure hook `phase` enum fix): already implemented, verified, and committed during P0** — recorded here for scope completeness; no further work item.

---

## Out of Scope

- 0000013 (setup refresh skill) — explicitly deferred to next release by the human, not discarded. Remains in `plan/backlog/`.
- Any change to `planifest-framework/scripts/consistency-check.mjs` — req-005 (0000018) is explicitly a *new*, separately-scoped script; extending `consistency-check.mjs` was the first-edition review's rejected approach (couples a per-feature gate to a repository-wide invariant).
- Any change to the `telemetry-mcp` backend server itself (`/Users/martinmayer/d/planifest/telemetry-mcp/`) — that is a separate repository outside this framework's source; 0000027's fix is entirely on the hook (emitting) side.
- Root-causing *why* the telemetry backend's earlier HTTP 400s occurred as a persistent-vs-transient question beyond the one root cause found (the invalid `phase` enum value) — that root cause fully explains the failure; no further investigation is in scope.

---

## Deferred

- 0000019 (populate the regression pack) — blocked until picked up in a future P0; nothing in this batch depends on it.
- 0000020 (decompose the orchestrator skill) — blocked until 0000019 lands (a populated regression pack is the safety net this decomposition needs).
- 0000021 (define a minimal artifact set) — needs a human judgement call on which artifacts are load-bearing; not agent-inferrable.
- 0000022 (token accounting per phase) and 0000023 (publish baseline comparison) — sequenced pair; 0000023 needs 0000022's metrics.
- 0000024 (record a skill-scope principle ADR) — low severity, strategic; governs future skill additions rather than fixing anything broken now.
- 0000025 (declare adoption position and stability policy) — needs the human to decide what Planifest's positioning is before anything can be written down.

---

*Generated by spec-agent. Path: `plan/current/scope.md`*

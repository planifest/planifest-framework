# Recommendations - 0000023-framework-pipeline-fixes

**Skill:** [docs-agent](../../planifest-framework/skills/planifest-docs-agent/SKILL.md)
**Feature:** 0000023-framework-pipeline-fixes
**Date:** 2026-08-02

## Recommendations for Future Iterations

1. **Refresh `.claude/skills/` via a skill-sync/setup re-run.** This feature edited the canonical source at `planifest-framework/skills/planifest-orchestrator/SKILL.md` and `planifest-framework/skills/planifest-ship-agent/SKILL.md`. `.claude/skills/` (the copy actually loaded when a skill is invoked in this project) is a synced build artifact — confirmed stale mid-session in this very run (the orchestrator content loaded at skill-invocation time still had the pre-0000022 per-phase-section format, missing the Phase Invocation Table entirely). Until a setup/skill-sync re-run happens, this project's own live orchestrator behavior does not reflect either 0000022's table consolidation or this feature's continuous_run/marker-lifecycle fixes.

2. **Prioritize the `cline.sh` bug discovered during this feature's P3.** `setup/cline.sh` sets `TOOL_SKILLS_DIR=".clinerules/skills"` and `TOOL_BOOT_FILE=".clinerules"` to colliding paths, crashing `setup.sh all` once the skills dir is created. This was previously masked by the copilot self-copy crash (fixed in this feature) aborting the `all` run first — now unmasked, it's the sole reason `setup.sh all` still can't reach exit 0. Out of scope for this feature (touching `cline.sh` was explicitly excluded from req-003); filed as backlog `0000034`. Recommend picking this up promptly since it affects every future `setup.sh all` invocation, not just this repo.

3. **`docs/decisions-index.md` has a drift gap predating this feature**, discovered while updating it here: the "Last updated" header still read `0000019-self-description-and-session-hygiene-fixes` despite feature 0000020's ADR section already being present in the body (added but the header wasn't bumped), and features 0000021 and 0000022 have no ADR sections in the index at all despite both having ADRs in their respective `plan/_archive/{feature-id}/adr/` folders. This feature's own P6 pass corrected the header (now `0000023-framework-pipeline-fixes`) and added its own ADR-001 entry, but did not backfill 0000020's missed bump or 0000021/0000022's missing sections — that's pre-existing drift unrelated to this feature's scope. Recommend a follow-up pass (or a deterministic check in `consistency-check.mjs`) to backfill the missing 0000021/0000022 entries and verify no other feature has a similar gap.

4. **Consider a lightweight backlog-entry accuracy check.** Backlog 0000031's filed text stated the continuous_run regression was "pre-existing behaviour, not introduced by feature 0000022" — true as far as it went, but incomplete: this feature's P0 investigation (at the human's explicit request) traced it one commit further back to feature 0000021 as the actual origin. The filing agent's own git-history check stopped one commit short. Not a process failure worth a new hard rule, but worth noting: when a backlog entry makes a specific historical claim ("pre-existing," "not introduced by X"), a future picker-up should treat that claim as a starting hypothesis to verify, not a settled fact, especially when the claim is doing load-bearing work in scoping the fix (as it nearly did here — the original framing invited "should this be a deliberate policy?" instead of "this is a regression to revert").

---
title: "ADR 006: Retroactive release tags delivered as a Planifest migration"
summary: "Use the Planifest migration mechanism to apply historical git tags rather than a standalone script or manual instructions."
status: "accepted"
version: "0.1.0"
---
# ADR-006 - Retroactive release tags delivered as a Planifest migration

**Skill:** [adr-agent](../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000012-docs-restructure-commit-directives
**Component:** planifest-framework
**Status:** accepted
**Date:** 2026-05-18

---

## Context

The repository has no git tags for previous releases (v0.1 through v0.10). These need to be applied retroactively to the merge-to-main commits for each release. The question is the delivery mechanism: a standalone script, manual instructions in a README, or a Planifest migration file.

The tagging operation is a one-time, destructive-if-wrong action (tags cannot be silently overwritten) that requires human confirmation of the commit→version mapping before execution. It is exactly the class of operation Planifest migrations are designed for.

---

## Decision

Deliver the retroactive tagging as a migration file at `planifest-framework/migrations/retroactive-release-tags.md`, processed by the `planifest-migrator` skill.

The migration:
1. Runs `git log --oneline --merges` and presents the output for the human to map commits to versions
2. Requires explicit human confirmation of each commit→version pair before creating the tag
3. Creates tags locally only (`git tag {version} {sha} -m "{version}"`)
4. Instructs the human to push tags separately (`git push origin --tags`)

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Standalone shell script | Self-contained; easy to run | No human-in-the-loop confirmation protocol; could be run without review; not idiomatic for Planifest | Rejected — bypasses the confirmation pattern the framework enforces for destructive operations |
| Manual instructions in CHANGELOG or README | No tooling required | Easy to skip; no enforcement; depends on human running correct git commands | Rejected — too easy to make mistakes with commit SHAs |
| Inline instructions in a commit message | Discoverable via git log | Not actionable; requires human to extract and interpret | Rejected — not a reliable delivery mechanism |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| planifest-framework | New migration file added to planifest-framework/migrations/; processed by migrator skill at next session start |

---

## Consequences

**Positive:**
- Human confirmation is built into the migration protocol — tags are never created without review
- Migration is idiomatic Planifest — other framework maintainers will recognise the pattern
- Migration is automatically detected by the orchestrator at the next session start (pending migrations check)

**Negative:**
- Requires the planifest-migrator skill to be available; cannot be executed without the Planifest toolchain
- One additional file in planifest-framework/migrations/ that must be moved to _done/ after execution

**Risks:**
- If `git log --merges` output is ambiguous (e.g. squash merges without "Merge" in the message), the human may not be able to identify all release commits — mitigated by the explicit confirmation step

---

## Related ADRs

- None

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent.*

---
title: "ADR 002: Formal P9 Ship phase — pipeline extended to P0–P9"
summary: "Add P9 as a formal terminal phase for the shipping action, extending the pipeline from P0–P8 to P0–P9 and renaming P7 to Archive."
status: "accepted"
version: "0.1.0"
---
# ADR-002 - Formal P9 Ship phase — pipeline extended to P0–P9

**Skill:** [adr-agent](../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000012-docs-restructure-commit-directives
**Component:** planifest-framework
**Status:** accepted
**Date:** 2026-05-18

---

## Context

The original pipeline ran P0–P8. P7 (Ship) was responsible for archiving `plan/current/`, writing the changelog, invoking P8 (Build Assessment) as a sub-call, and raising the PR via `gh pr create` — all in one phase. This created three problems:

1. **Phase boundary ambiguity:** P7 wrapped P8, making the phase sequence unclear to agents. Some agents inferred a "P9" to describe the PR step that happened after P8, even though it was never defined.
2. **Semantic mismatch:** "Ship" describes the act of shipping (raising the PR), but P7 spent most of its work archiving and tidying — not shipping.
3. **Inflexibility:** `gh pr create` was hardcoded; users who push and raise PRs themselves (e.g. due to SSH passphrase requirements) had no clean path.

---

## Decision

Extend the pipeline to P0–P9 with explicit phase names:

| Phase | Name | Responsible Skill |
|-------|------|-------------------|
| P7 | Archive | ship-agent |
| P8 | Build Assessment | build-assessment-agent (sub-agent) |
| P9 | Ship | ship-agent |

P7 handles all close-out groundwork (changelog, archive, `.feature-id`). P8 analyses the build. P9 is the explicit shipping act (git tag + PR).

The Response Prefix Convention table in the orchestrator is annotated as the **complete and exhaustive** list of phases. A Hard Limit is added: no phase number outside P0–P9 may appear in agent output.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Keep P0–P8; PR at end of P8 | No new phase number | P8 (Build Assessment) takes on PR responsibility — conceptually wrong; assessment ≠ shipping | Rejected — mixing concerns |
| Keep P0–P8; PR stays in P7 post-P8 | Fewest changes | Phase boundary ambiguity remains; agents still invent P9 | Rejected — root cause unaddressed |
| Add P9 as a separate standalone phase invoked by orchestrator (not ship-agent) | Clean orchestrator control | Ship-agent loses ownership of the full close-out sequence; ship-agent becomes partial | Rejected — ship-agent should own the complete close-out |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| planifest-orchestrator/SKILL.md | Phase table updated P0–P9; P7 renamed Archive; P9 section added; Hard Limit added |
| planifest-ship-agent/SKILL.md | Restructured into P7 Archive + P8 sub-agent spawn + P9 Ship |
| planifest-build-assessment-agent/SKILL.md | Clarified as sub-agent invoked by ship-agent |
| planifest-framework/pipeline-reference.md | Phase list updated to P0–P9 |

---

## Consequences

**Positive:**
- Phase sequence is unambiguous — agents cannot invent phases because the table is exhaustive and a Hard Limit enforces it
- "Ship" now semantically means the shipping act; "Archive" means the close-out groundwork
- Human push/PR path is explicitly supported in P9

**Negative:**
- All existing documentation and training material referencing P0–P8 requires updating
- Users familiar with the old model must re-learn that P7 is no longer "Ship"

**Risks:**
- If agents read cached/stale skill content, they may continue to use the old P7 Ship label — mitigated by setup.sh re-run after framework update

---

## Related ADRs

- ADR-003 — depends-on (ship-agent orchestration depends on this phase structure)
- ADR-004 — depends-on (P9 PR protocol depends on P9 being a formal phase)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent.*

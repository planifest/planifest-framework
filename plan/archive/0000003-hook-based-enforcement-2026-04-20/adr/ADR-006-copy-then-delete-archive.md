---
title: "ADR-006: Copy-then-delete archive strategy for plan/current/"
summary: "The ship-agent copies plan/current/ to the archive destination before deleting the source. An atomic rename (move) was rejected because partial deletion is harder to recover from than having artefacts in both locations."
status: "accepted"
version: "0.1.0"
---
# ADR-006 - Copy-then-delete archive strategy for plan/current/

**Skill:** [adr-agent](../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-sonnet-4-5
**Feature:** 0000003-hook-based-enforcement
**Component:** planifest-framework/skills/planifest-ship-agent/
**Status:** accepted
**Date:** 2026-04-18

---

## Context

At the end of Phase 7 (Ship), the ship-agent must archive `plan/current/` to `plan/archive/{feature-id}-{YYYY-MM-DD}/` so the workspace is clean for the next feature. A failed or interrupted archive must leave the system in a recoverable state.

Two strategies exist: copy-then-delete (two operations) or atomic rename/move (one operation). On most operating systems, `fs.rename` is atomic within the same filesystem but not across filesystems or volumes. In cloud-synced or containerised environments (common in agentic tool setups), `plan/current/` and `plan/archive/` may not be on the same underlying filesystem.

The failure modes differ significantly:
- **Failed copy:** source is intact; archive may be partial or absent. Fully recoverable — re-run ship.
- **Failed atomic move / partial rename:** if the OS renames a directory entry but fails mid-way on nested files (cross-filesystem case), `plan/current/` may be partially deleted with no archive to recover from.

---

## Decision

Use copy-then-delete:
1. Write `plan/current/.feature-id` with the feature ID (marker for resume detection)
2. Recursively copy all files from `plan/current/` to `plan/archive/{feature-id}-{YYYY-MM-DD}/`
3. If the target archive path already exists, use `{feature-id}-{YYYY-MM-DD}-{n}` (n=2,3,…) to avoid collision
4. Only after the copy is fully confirmed: delete `plan/current/` contents
5. Confirm empty `plan/current/` before declaring Ship complete

**Idempotency:** If step 4 fails mid-delete, `plan/current/` still contains artefacts. On re-run, ship-agent detects the archive already exists (via `.feature-id`), adds a `-{n}` suffix, and proceeds with a fresh copy-then-delete. The duplicate archive is harmless.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Atomic rename (`fs.rename`) | Single syscall; fastest | Not atomic across filesystems; partial-rename state is unrecoverable without manual intervention | Recovery from failure is worse than copy-then-delete |
| Git mv / git commit the archive | Auditable in git history | Git operations in the middle of ship create a checkpoint commit mid-phase; complicates git log | Ship-agent already writes a changelog commit; double-commit mid-phase adds noise |
| Leave `plan/current/` in place; rely on `.feature-id` for state | No deletion risk | Artefacts from prior feature contaminate next feature; resume detection becomes complex | Defeats the purpose of archiving |
| Delete-then-copy (reverse order) | Same I/O cost | Source deleted before archive confirmed; data loss if copy fails | Strictly worse failure mode |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| `planifest-framework/skills/planifest-ship-agent/SKILL.md` | Ship process steps specify copy-then-delete order |
| `plan/current/.feature-id` | Written at step 1; enables resume detection to identify stale artefacts |
| `planifest-framework/skills/planifest-orchestrator/SKILL.md` | Resume detection reads `.feature-id` to detect stale artefacts |

---

## Consequences

**Positive:**
- Any failure leaves either the source or the archive (or both) fully intact — no data loss scenario
- Idempotent: re-running ship after a failed archive always produces a clean result
- `.feature-id` marker enables resume detection to identify stale artefacts without false positives

**Negative:**
- Two filesystem operations instead of one; on large `plan/current/` directories (unlikely but possible), copy is slower
- After a partial failure, `plan/current/` and the partial archive both contain files — a human may be confused by the duplicate state

**Risks:**
- The collision suffix logic (`-{n}`) must handle the case where the archive at suffix `-1` was from a failed run (partial). The ship-agent should always append a new suffix rather than attempting to reuse an existing archive directory.

---

## Related ADRs

- ADR-007 - related-to (resume detection relies on `.feature-id` written during archive)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent. Path: `plan/current/adr/ADR-006-copy-then-delete-archive.md`*

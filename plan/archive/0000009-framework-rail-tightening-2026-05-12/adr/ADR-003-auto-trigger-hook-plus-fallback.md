---
title: "ADR-003: Auto-trigger orchestrator via UserPromptSubmit hook with CLAUDE.md fallback"
summary: "The orchestrator is triggered automatically at session start using a UserPromptSubmit hook for Claude Code, with a CLAUDE.md instruction as a fallback for tools that lack hook support."
status: "accepted"
version: "0.1.0"
---
# ADR-003 - Auto-trigger orchestrator via UserPromptSubmit hook with CLAUDE.md fallback

**Skill:** planifest-adr-agent
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000009-framework-rail-tightening
**Component:** planifest-framework
**Status:** accepted
**Date:** 09 May 2026

---

## Context

Currently, users must explicitly invoke the orchestrator at the start of every session in a planifest project. This is a friction point — experienced users remember, new users do not, and sessions started without the orchestrator bypass the framework's discipline entirely.

Planifest already has a `UserPromptSubmit` hook mechanism (used by `check-design.mjs`) that fires on every user prompt in Claude Code. A similar hook could fire on the first prompt of a session and check whether the orchestrator should be loaded.

The challenge: not all tools support `UserPromptSubmit`. Cursor, Windsurf, Cline, and others lack this hook. The solution must degrade gracefully.

A secondary challenge: the hook must not fire repeatedly within the same session (once the orchestrator is active, further `UserPromptSubmit` events must be no-ops).

---

## Decision

A new hook script (`auto-trigger-orchestrator.mjs`) is installed by setup alongside existing enforcement hooks. It is registered as a `UserPromptSubmit` handler in `.claude/settings.json`. On each fire, it:
1. Checks whether `planifest-framework/` exists in the current working directory (planifest project detection)
2. Checks whether `plan/.orchestrator-active` exists (already active — exit 0 silently)
3. If both conditions are met (planifest project, not yet active), outputs an instruction to the model to load the `planifest-orchestrator` skill

In parallel, a CLAUDE.md instruction is added as a fallback for tools without `UserPromptSubmit` support: "At the start of every session in a planifest project (planifest-framework/ present), load the planifest-orchestrator skill."

The hook is installed by both `setup.sh` and `setup.ps1` as part of standard Claude Code setup (no new flag).

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Hook only (no CLAUDE.md fallback) | Single mechanism; no instruction bloat in CLAUDE.md | Non-Claude Code tools get no auto-trigger benefit | Leaves Cursor, Windsurf, Cline users without the feature |
| CLAUDE.md instruction only (no hook) | Works across all tools that read CLAUDE.md | Relies on model compliance — advisory, not enforced; model may not always follow it | Hook is more reliable for Claude Code; instruction-only is weaker |
| SessionStart hook instead of UserPromptSubmit | Fires once per session cleanly | Claude Code does not expose a SessionStart hook in the same way; UserPromptSubmit is the available hook event for this | Platform constraint |
| Require explicit invocation (status quo) | Zero hook complexity | Friction; sessions bypass orchestrator silently | The problem this feature is solving |
| Auto-inject orchestrator SKILL.md content at session start | No hook needed | Would require forking the tool's boot mechanism; not extensible | Too invasive; not a supported extension point |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| planifest-framework | New hook script `auto-trigger-orchestrator.mjs` in `hooks/enforcement/`; setup.sh and setup.ps1 register it in `UserPromptSubmit` settings; CLAUDE.md updated with fallback instruction |

---

## Consequences

**Positive:**
- Claude Code users get automatic orchestrator invocation with no manual step
- Non-Claude Code users get the CLAUDE.md advisory instruction as a fallback
- Idempotency via `plan/.orchestrator-active` sentinel prevents repeated invocation within a session
- Consistent session start behaviour regardless of whether the user remembers to load the orchestrator

**Negative:**
- An additional `UserPromptSubmit` hook fires on every prompt, adding marginal latency (hook is a fast file-existence check — sub-millisecond)
- CLAUDE.md instruction grows slightly; advisory instructions are weaker than hooks

**Risks:**
- If `plan/.orchestrator-active` is left stale (not deleted by P7), the hook fires on every session in an already-shipped project; mitigated by P7 cleanup deleting the sentinel

---

## Related ADRs

- None

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by planifest-adr-agent.*

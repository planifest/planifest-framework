---
title: "Scope - framework-governance"
status: "draft"
version: "0.1.0"
---
# Scope - 0000005-framework-governance

**Feature:** 0000005-framework-governance
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Version:** 0.1.0

---

## In Scope

- `planifest-framework/standards/library-standards/` — new directory tree; one subdir per language (21 languages) plus `databases/`; each contains `prefer-avoid.md` and `test-frameworks.md`; `_version-policy.md` at root
- `planifest-overrides/` — new repo-root directory (next to `planifest-framework/`); human-owned, never touched by framework upgrades; contains `library-standards/`, `capability-skills/`, `instructions/` subdirs with `.gitkeep`
- `planifest-framework/standards/formatting-standards.md` — new standards doc covering date format (DD MMM YYYY body / YYYY-MM-DD filenames), British English locale (with identifier exceptions), response verbosity rules, and multilingual deferral notice
- `planifest-framework/hooks/enforcement/gate-write.mjs` — extend with sentinel check blocking `plan/current/**` writes (except `feature-brief.md`) when `plan/.orchestrator-active` is absent
- `planifest-framework/hooks/enforcement/check-design.mjs` — extend to inject hard STOP when no feature-brief and no sentinel present
- `planifest-framework/hooks/adapters/copilot.mjs` — new adapter wiring gate-write and check-design into Copilot Agent Hooks (Preview 2025); degrades gracefully when hooks disabled
- `planifest-framework/skills/planifest-orchestrator/SKILL.md` — extend: write sentinel at P0 start; read `planifest-overrides/instructions/` and write `## Repo Instructions` to design.md at P0; scan skills-inbox at every phase transition; maintain `## Active Skills` in design.md; scan migrations/ at session start
- `planifest-framework/skills/planifest-codegen-agent/SKILL.md` — extend: library-standards lookup (custom-first fallback) before any dependency manifest; substitute avoided libraries
- `planifest-framework/skills/planifest-validate-agent/SKILL.md` — extend: library audit CI check against avoid lists
- `planifest-framework/skills/planifest-ship-agent/SKILL.md` — extend: archive plan-scoped capability skills at P7; delete `plan/.orchestrator-active`
- `planifest-framework/skills/planifest-migrator/SKILL.md` — new skill; reads migration file, executes interactively, moves to `_done/` on completion
- `planifest-framework/skills-inbox/` — new drop-zone directory (`.gitkeep`)
- `planifest-framework/capability-skills/` — new permanent capability skills directory (`.gitkeep`)
- `planifest-framework/migrations/` and `_done/` — new migration directories
- `planifest-framework/migrations/0001-date-format.md` — migration: scan and correct non-compliant body-text dates
- `planifest-framework/migrations/0002-british-english.md` — migration: scan and correct American English prose spellings
- `planifest-framework/external-skills.json` — generated by setup scanning `planifest-overrides/capability-skills/`; initially `{}`
- `planifest-framework/templates/execution-plan.template.md` — add `## Active Skills` section
- setup.ps1 / setup.sh — scan `capability-skills/`, regenerate `external-skills.json`, register Copilot adapter
- All prose in `planifest-framework/` rewritten to British English spellings during P3

---

## Out of Scope

- Automated version freshness checks against npm/PyPI/PyPI at runtime
- Changes to any application `src/` code
- Roo-Code enforcement — no hook API; tool is sunset 02 May 2026
- Cursor adapter audit — `cursor.mjs` retained as-is; verification deferred

---

## Deferred

- **Full library-standards content for 16 of 21 languages** — P3 fully populates TypeScript, JavaScript (React + Node.js sub-sections), Python, Go, and Java; remaining 16 get stub files with `TODO: populate` notes. No functionality is blocked — agents fall back gracefully when a language stub is present.
- **Multilingual support** — `formatting-standards.md` documents British English as the current default and states multilingual support is planned for a future release. No implementation in this feature.
- **Cursor adapter audit** — existing `cursor.mjs` hook adapter is retained unchanged. A separate audit to verify enforcement actually functions in Cursor is deferred.

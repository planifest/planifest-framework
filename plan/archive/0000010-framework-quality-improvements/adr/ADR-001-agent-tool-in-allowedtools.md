---
title: "ADR 001: Agent tool added to allowedTools for Claude Code projects"
summary: "Adding 'Agent' to the project-scoped allowedTools in .claude/settings.json written by setup.sh/setup.ps1 removes the per-use confirmation gate for sub-agent spawning, enabling the Subagent Decomposition Directive to be followed in practice."
status: "accepted"
version: "0.1.0"
---
# ADR-001 - Agent tool added to allowedTools for Claude Code projects

**Skill:** planifest-adr-agent
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000010-framework-quality-improvements
**Component:** setup-hook-integration
**Status:** accepted
**Date:** 2026-05-12

---

## Context

The Planifest orchestrator SKILL.md includes a Subagent Decomposition Directive instructing phase agents to dispatch independent tasks as parallel Agent tool calls. In feature 0000009, the P8 build report confirmed that 0 Agent calls were made across all 8 phases. Investigation revealed the root cause: the `Agent` tool was not listed in `allowedTools` in `.claude/settings.json`. In Claude Code's default permission mode, any tool not in `allowedTools` requires explicit user confirmation per invocation. Since no confirmation prompt appeared in the pipeline and no approval was recorded, the Agent tool was silently bypassed — all phase work ran inline.

The question: should `Agent` be added to `allowedTools` (removing per-use confirmation) or should the directive be changed to accept that Agent calls require user approval each time?

---

## Decision

Add `"Agent"` to the `allowedTools` array written by `setup.sh` and `setup.ps1` when configuring for Claude Code. The addition is project-scoped (written to `.claude/settings.json`, not the global Claude Code settings) and is idempotent.

The directive requires Agent dispatch. The only reason it was not followed was a missing permission entry. The fix is the missing entry, not the directive.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Keep current (no allowedTools entry) | No change to permission model | Agent tool never used in practice; directive is unenforceable | Root cause of the problem — rejected |
| Add to global Claude Code settings | Removes friction across all projects | Affects projects outside Planifest; not appropriate to modify global settings from a project setup script | Scope violation |
| Update directive to require user to manually approve each Agent call | Transparent to user | Breaks continuous_run; requires human at every phase for every parallel dispatch; defeats the purpose | Defeats parallelism |
| Remove Subagent Decomposition Directive | Simplifies skills | Loses the efficiency benefit the directive was designed to deliver | Retreats from the design intent |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| setup-hook-integration | `setup.sh` and `setup.ps1` gain allowedTools write logic for Claude Code target |
| planifest-framework/skills | SKILL.md files now have a live Agent dispatch path once allowedTools is set |

---

## Consequences

**Positive:**
- Agent tool can be invoked without per-use confirmation prompts in Planifest projects
- Subagent Decomposition Directive becomes enforceable and verifiable in P8 build reports
- Phase agents can dispatch parallel Agent calls in continuous_run without blocking

**Negative:**
- Agent invocations in Planifest projects no longer require per-use human confirmation — an agent acting outside the skill directives could spawn subagents unexpectedly
- Setup must merge allowedTools entries rather than overwrite, adding complexity to the setup script logic

**Risks:**
- A buggy or misloaded skill could trigger unintended Agent spawning; mitigated by the fact that Agent calls still require a deliberate `Agent(...)` invocation in the skill code, not a passive hook

---

## Related ADRs

- None

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent. Path: `plan/current/adr/ADR-001-agent-tool-in-allowedtools.md`*

---
title: "ADR 001: PreToolUse Block Mechanism — hookSpecificOutput deny vs exit code 2"
summary: "We use hookSpecificOutput.permissionDecision: 'deny' + permissionDecisionReason to block tool calls, rather than exit code 2. This surfaces a specific redirect message to the agent instead of a raw stderr error string."
status: "accepted"
version: "0.1.0"
---
# ADR-001 - PreToolUse Block Mechanism

**Skill:** adr-agent
**Tool:** claude-code
**Model:** claude-sonnet-4-6
**Feature:** 0000001-context-mode-enforcement-hooks
**Component:** context-mode-hooks
**Status:** accepted
**Date:** 2026-04-12

---

## Context

Claude Code `PreToolUse` hooks have two distinct mechanisms for blocking a tool call:

1. **Exit code 2** — the script exits with code 2; Claude Code ignores stdout and feeds stderr text back to Claude as a raw error message.
2. **hookSpecificOutput JSON** — the script exits 0 and outputs `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"..."}}` to stdout. Claude Code presents the `permissionDecisionReason` to the agent as the block rationale.

The primary goal of this feature is not just to prevent the tool call — it is to redirect the agent to the correct `ctx_*` replacement tool. The block message must be actionable, not an error.

A third option — top-level `decision: "block"` / `reason` — exists in the docs but is marked deprecated for `PreToolUse`. It must not be used.

---

## Decision

Use `hookSpecificOutput.permissionDecision: "deny"` with a specific `permissionDecisionReason` that names the exact `ctx_*` replacement call.

The `permissionDecisionReason` must include:
- The name of the forbidden tool
- The exact `ctx_*` replacement with call syntax
- The original input (command / URL / pattern) so the agent can reconstruct the equivalent call without re-reading the tool invocation

Exit 0 in all cases — including the deny path.

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Exit code 2 + stderr message | Simple — no JSON output required; guaranteed to block | stderr text is presented as a raw error, not as a structured instruction; agents may treat it as a transient failure and retry rather than redirect | Blocks without redirecting — the feature's core value is the redirect, not just the block |
| Top-level `decision: "block"` / `reason` | Slightly shorter JSON output | Deprecated for `PreToolUse` in Claude Code docs; may be removed in a future release | Using deprecated APIs creates a maintenance liability with no benefit |
| `continue: false` + `stopReason` | Stops Claude entirely | Too aggressive — stops the entire session, not just the one tool call | Disproportionate response; blocks all work, not just the offending call |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| context-mode-hooks | All three hook scripts (block-grep.sh, block-bash.sh, block-webfetch.sh) must output `hookSpecificOutput` JSON on exit 0 |
| setup-hook-integration | No direct impact on wiring — the settings.json hook entry format is the same regardless of output mechanism |

---

## Consequences

**Positive:**
- The agent receives an actionable redirect instruction, not a raw error — it can immediately construct and call the correct `ctx_*` tool
- Using the current (non-deprecated) API ensures compatibility with future Claude Code versions
- Exit 0 in all paths means no risk of the hook process being interpreted as a system error

**Negative:**
- Scripts must parse and emit JSON — more code than a simple `exit 2` + `echo "error"` approach
- The `hookSpecificOutput` envelope adds verbosity; the JSON structure must be exact or Claude Code will ignore it

**Risks:**
- If Claude Code silently changes the `hookSpecificOutput` schema in a future version, blocks will fail silently (exit 0 with unrecognised JSON = allow). Mitigated by: smoke test in validate phase that confirms a deny is actually surfaced to the agent.

---

## Related ADRs

- ADR-002 - related-to (allowlist affects what the deny path is triggered on for Bash)
- ADR-003 - depends-on (blocking strategy determines how often this output format fires)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent.*

# Execution Plan - context-mode plugin routing rules

> Written by the spec-agent. Derived from the Feature Brief - not invented. Every requirement must be traceable to a user story or acceptance criterion.

**Skill:** planifest-spec-agent
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000008-context-mode-plugin-routing-rules
**Phase:** 1 (single phase)
**Version:** 0.1.0
**Status:** active

---

## Functional Requirements Directory

Functional requirements are split into granular files to optimise agent context windows.

See `plan/current/requirements/` for individual feature requirements.

| Req ID | Slug | Summary |
|--------|------|---------|
| REQ-001 | remove-template-files | Delete all context-mode-agents.md template files |
| REQ-002 | update-setup-scripts | Remove routing rules copy step; update help text |
| REQ-003 | update-documentation | All docs describe plugin as canonical source of routing rules |

---

## Non-Functional Requirements

| ID | Category | Requirement | Target | Measurement |
|----|----------|------------|--------|-------------|
| NFR-001 | Consistency | No stale references to `context-mode-agents.md` or `AGENTS.md` as a routing rules file remain anywhere in live docs, scripts, or templates | Zero matches on audit grep | `grep -r 'context-mode-agents' planifest-framework/ .claude/skills/` returns empty |
| NFR-002 | Correctness | The `--context-mode-mcp` flag must still successfully install enforcement hooks after routing rules copy step is removed | Hook scripts present and registered after setup run | `.claude/hooks/context-mode/block-*.sh` exist and appear in `.claude/settings.json` PreToolUse |
| NFR-003 | Idempotency | Re-running setup with `--context-mode-mcp` must not write any routing rules file | No `AGENTS.md` written by setup | File watcher / diff confirms no AGENTS.md created |

---

## API Summary

Not applicable — no API surface.

---

## Data Model Summary

Not applicable — no data model.

---

## Component Interactions

Not applicable — this change is confined to framework files (scripts, templates, docs).

---

## Assumptions

| ID | Assumption | Impact if Wrong |
|----|-----------|----------------|
| A-001 | The context-mode plugin injects routing rules for all tools that support the plugin system, not just Claude Code | Non-Claude Code tools (Cursor, Windsurf, etc.) lose routing rules with no fallback — would need per-tool routing rule files reinstated |
| A-002 | AGENTS.md in the project root is used as a Codex boot file only and not as a context-mode routing rules file | Codex users who relied on the routing rules written to AGENTS.md would silently lose them |

---

## Open Questions

None — all material gaps are documented as assumptions (A-001, A-002) and tracked in the risk register.

---

*Generated by spec-agent.*

# Execution Plan - framework-rail-tightening

> Written by the spec-agent. Derived from the Feature Brief - not invented. Every requirement must be traceable to a user story or acceptance criterion.

**Skill:** planifest-spec-agent
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000009-framework-rail-tightening
**Phase:** 1 of 3
**Version:** 0.1.0
**Status:** active

---

## Active Skills

| Skill | Scope | Purpose |
|-------|-------|---------|
| None | — | — |

---

## Functional Requirements Directory

Functional requirements are split into granular files to optimise agent context windows.

See `plan/current/requirements/` for individual feature requirements.

### Phase 1 (this phase)

| Req ID | Slug | Summary |
|--------|------|---------|
| REQ-001 | skips-path-fix | Fix bare `.skips` references in orchestrator SKILL.md to `plan/current/.skips` |
| REQ-002 | auto-trigger-orchestrator | Auto-trigger orchestrator at session start via UserPromptSubmit hook + CLAUDE.md fallback |
| REQ-003 | subagent-decomposition | Orchestrator instructs phase agents to decompose hard tasks into subagents with skill-library lookup |
| REQ-004 | skill-requirement-mapping | Orchestrator produces and maintains skill-to-requirement map from P0; re-evaluated at each gate |
| REQ-005 | open-source-skill-library | Curated open-source skill library with --include-full-skill-library flag and attribution.txt per skill |
| REQ-006 | pause-resume | Orchestrator writes pause.md on command; resume detection restores from exact pause point |
| REQ-007 | gate-write-windows-fix | Fix gate-write.mjs Windows path-separator bug in source hook file |

### Phase 2 (next phase — not specced yet)

| Req ID | Slug | Summary |
|--------|------|---------|
| REQ-008 | override-instructions-injection | setup.ps1 and setup.sh inject planifest-overrides/instructions/ into orchestrator SKILL.md and workflow files |
| REQ-009 | bash-append-override-parity | setup.sh Append-OverrideInstructions parity with PS1 |
| REQ-010 | bash-capability-skills-parity | setup.sh Copy-CapabilitySkills parity with PS1 |
| REQ-011 | ps1-tier1-adapters | setup.ps1 Tier 1 adapter support for Cursor, Windsurf (conditional), Cline, roo-code |
| REQ-012 | ps1-opencode-support | setup.ps1 opencode tool support |

### Phase 3 (deferred)

| Req ID | Slug | Summary |
|--------|------|---------|
| REQ-013 | typescript-adapter-opencode | TypeScript adapter for OpenCode/KiloCode enforcement |

---

## Non-Functional Requirements

| ID | Category | Requirement | Target | Measurement |
|----|----------|------------|--------|-------------|
| NFR-001 | Correctness | gate-write.mjs always-permitted check passes for `plan/` and `plan/archive/` paths on Windows | Zero blocked writes to always-permitted paths | Regression test: write to plan/archive/ with no design.md present → expect exit 0 |
| NFR-002 | Idempotency | setup.sh re-run produces no duplicate entries in boot file or skill files | Diff shows no change on second run | Re-run test in test suite |
| NFR-003 | Consistency | All bare `.skips` references in orchestrator SKILL.md replaced | Zero matches for `^\.skips` or `\s\.skips` not preceded by `plan/current/` | Grep audit post-implementation |
| NFR-004 | Attribution | Every skill in external-skills/ has an attribution.txt; no skill without permissive license | 100% attribution coverage; zero non-permissive licenses | Pre-ship grep + license check |

---

## API Summary

Not applicable — no API surface.

---

## Data Model Summary

Not applicable — no data model changes.

---

## Component Interactions

```
planifest-overrides/instructions/ → setup.sh/setup.ps1 → orchestrator SKILL.md (injected)
planifest-framework/external-skills/ → setup.sh/setup.ps1 → tool skill dir (copied when flag set)
UserPromptSubmit hook → planifest-orchestrator → plan/.orchestrator-active sentinel
plan/current/pause.md ← orchestrator (on command) → resume detection (on next session start)
gate-write.mjs → plan/archive/ write → pass (Windows path normalised)
```

---

## Assumptions

| ID | Assumption | Impact if Wrong |
|----|-----------|----------------|
| A-001 | Curated open-source skills found via web search have identifiable permissive licenses (MIT, Apache 2.0, ISC, BSD) | Library ships with fewer skills; none without confirmed license |
| A-002 | The orchestrator SKILL.md source file is the canonical location; setup copies it to tool dirs — so editing source propagates on next setup run | If setup doesn't re-copy the orchestrator SKILL.md, injected instructions won't appear in the tool's copy |
| A-003 | Claude Code UserPromptSubmit hook fires reliably on every session start | If hook is unreliable, CLAUDE.md fallback catches it; no hard failure |

---

## Open Questions

None — all material gaps resolved in P0 coaching.

---

*Generated by planifest-spec-agent.*

---
title: "Scope - Framework Context Bloat Audit"
summary: "Defines explicit boundaries of what is in scope and out of scope."
status: "active"
version: "0.1.0"
---
# Scope - Framework Context Bloat Audit

**Skill:** [spec-agent](../../planifest-framework/skills/planifest-spec-agent/SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000021-framework-context-bloat-audit
**Wave:** 1 (single wave)
**Version:** 0.21.0

---

## In Scope

- Promoting existing tests from `planifest-framework/tests/` into `planifest-framework/tests/regression/`, human-reviewed, run once to record a baseline before any other work (req-001)
- A fresh-context `claude-opus-5` audit pass over every `planifest-framework/skills/*/SKILL.md`, every `planifest-framework/standards/*.md`, every `planifest-framework/templates/*.md`, and the root `CLAUDE.md`, producing a written findings report (req-002)
- Per-file trimming of content the findings report flags as redundant, restated, or model-implicit, gated by a second fresh-context reviewer checking two guardrails (no enforcement-content loss, no ambiguity/doom-loop regression), with a 5-attempt failure-informed retry before reverting a file to its original wording (req-003)
- Re-running the regression pack and full test suite after trimming, comparing against the baseline, and recording before/after line-count metrics plus the comparison in the changelog (req-004)

---

## Out of Scope

- Any file under `.claude/` — a synced copy installed by `setup.sh`'s skill-sync mechanism from `planifest-framework/`, never edited directly; refreshed separately by the human after this feature ships
- Structural decomposition of the orchestrator into a router + `references/` pattern (backlog `0000020`)
- Authoring a formal skill-scope principle ADR (backlog `0000024`)
- Conditional/minimal per-run artifact set changes to the pipeline itself (backlog `0000021`) — a different axis from this feature's instruction-content trim
- Any change to hook `.mjs` logic
- Any change to `planifest-framework/external-skills/` (third-party content)
- `operational-model.md`, `slo-definitions.md`, `cost-model.md` — not produced for this feature; no runtime component, no deployment topology, no cost drivers (consistent with precedent set by 0000009 and 0000010, prior framework-only meta-features)
- OpenAPI specification and data contract — not applicable; no API surface, no data store

---

## Deferred

- Orchestrator structural decomposition (backlog `0000020`) — blocked until this general bloat-removal pass lands first, per human direction during the P0 backlog pickup
- Skill-scope principle ADR (backlog `0000024`) — blocked until this general bloat-removal pass lands first, per human direction during the P0 backlog pickup

---

*Generated by spec-agent. Path: `plan/current/scope.md`*

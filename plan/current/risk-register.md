---
title: "Risk Register - Setup Refresh Skill"
summary: "Technical, operational, and security risks with their mitigations."
status: "active"
version: "0.1.0"
---
# Risk Register - Setup Refresh Skill

**Skill:** [spec-agent](../../planifest-framework/skills/planifest-spec-agent/SKILL.md) (updated by any agent that identifies a new risk)
**Tool:** claude-code
**Model:** claude-sonnet-4-6
**Feature:** 0000020-setup-refresh-skill
**Version:** 0.20.0
**Overall Risk Level:** medium

---

## Risks

| ID | Category | Description | Likelihood | Impact | Mitigation | Status |
|----|----------|------------|------------|--------|-----------|--------|
| R-001 | security | The skill's deletion allowlist (`CLAUDE.md`, `AGENTS.md`) is a hardcoded list that, if ever loosened by a future change, could delete user-owned files | low (by design, not editable at runtime) | high (data loss for the human on the loop) | REQ-004 requires the allowlist stay hardcoded and non-extensible; validate-agent must assert this with a dedicated test that fails if the allowlist is parameterised or widened | open |
| R-002 | operational | A process killed between boot-file deletion and setup completion leaves the repo with missing `CLAUDE.md`/`AGENTS.md` | low (requires a mid-run interruption) | medium (degraded repo state until the human notices and reruns) | REQ-009 writes the marker file before deletion begins; REQ-010 detects and recovers from this exact state on the next invocation | open |
| R-003 | technical | `setup.sh` and `setup.ps1` are historically prone to drift (see feature `0000011-setup-parity-and-consistency`); this feature's own marker-write addition could drift between the two scripts if not tested in parity | medium (pre-existing pattern in this codebase) | low (a drifted marker write degrades reconstruction confidence, it does not corrupt data — REQ-002 falls back to inference) | REQ-008's acceptance criteria require byte-for-byte equivalent marker content (modulo timestamp) between the two scripts, verified by a parity test | open |
| R-004 | technical | Flag reconstruction from hook wiring (REQ-002) is inherently heuristic when no marker file exists; a misread signal could produce a wrong flag at high confidence | low (the four known signals are direct 1:1 mappings to installed state) | medium (a wrong high-confidence flag could still be confirmed by an inattentive human) | REQ-003's confirmation gate always shows the full flag set and source for every run, including high-confidence ones — no flag is ever silently applied |  open |

---

## Assumptions Logged as Risks

| ID | Assumption | Impact if Wrong | Status |
|----|-----------|----------------|--------|
| A-001 | Installed hook wiring reliably signals which flags were used at install time | Reconstruction confidence is lower than expected; more runs require human confirmation of a lower-confidence set — this is the designed fallback, not a failure | open |
| A-002 | `.claude/.planifest-setup-flags`, once present, is preferred over hook-wiring inference with no staleness check against current hook wiring | A hand-edited hook wiring change without re-running setup could leave the marker reporting a stale flag set; no reconciliation is built for this in scope | open |

---

*Generated by spec-agent. Updated by any agent that identifies a new risk. Path: `plan/current/risk-register.md`*
